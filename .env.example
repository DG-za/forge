# PostgreSQL connection string (Prisma + pg pool)
DATABASE_URL="postgresql://forge:forge@localhost:5432/forge"

# Anthropic API key — used for Claude-based planner/coder/reviewer agents
ANTHROPIC_API_KEY="sk-ant-..."

# OpenAI API key — used for OpenAI-based planner/coder/reviewer agents
OPENAI_API_KEY="sk-..."

# --- Optional ---
# No optional variables yet. Add them here as they're introduced.
